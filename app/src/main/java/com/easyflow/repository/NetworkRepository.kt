package com.easyflow.repository

import com.easyflow.database.models.UserDatabaseModel
import com.easyflow.network.Network
import okhttp3.ResponseBody
import retrofit2.Response

class NetworkRepository {
   /* suspend fun signIn(user: UserDatabaseModel): Response<ResponseBody>{
        return Network.easyFlowServices.signIn(user)
    }

    suspend fun getUserInfo(authKey : String): Response<UserDatabaseModel>{
        return Network.easyFlowServices.getUserInfo(authKey)
    }
    suspend fun register(user: UserDatabaseModel): Response<ResponseBody>{
        return Network.easyFlowServices.register(user)
    }*/
}